[project]
name = "sentinel-feature-store"
version = "0.1.0"
requires-python = ">=3.12,<3.13"
dependencies = [
    "fastapi>=0.115",
    "uvicorn[standard]>=0.32",
    "pydantic>=2.9",
    "grpcio>=1.66",
    "redis>=5.1",
    "sqlmodel>=0.0.22",
    "asyncpg>=0.30",
    "sentinel-shared",
]

[tool.uv.sources]
sentinel-shared = { path = "../../shared", editable = true }

[dependency-groups]
dev = [
    "pytest>=8.3",
    "httpx>=0.27",
]
